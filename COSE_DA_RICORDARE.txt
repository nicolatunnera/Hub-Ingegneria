─═══ ENGINEERING CLOUD HUB — COSE DA RICORDARE ═══─

┌─────────────────────────────────────────────────────────────┐
│ DUE REPO:                                                   │
│  1. Hub-Ingegneria (GitHub Pages → Pubblico, Firebase)      │
│     URL: https://tommy-202.github.io/Hub-Ingegneria/        │
│     git remote: https://github.com/tommy-202/Hub-Ingegneria │
│     ↓ se modifichi ↓                                        │
│     cd C:\Users\User\Desktop\SITO                           │
│     git add . && git commit -m "msg" && git push            │
│                                                             │
│  2. Engineering-Hub (Render → Privato, nedb)                │
│     URL: https://engineering-hub.onrender.com               │
│     git remote: https://github.com/tommy-202/Engineering-Hub│
│     ↓ se modifichi ↓                                        │
│     cd C:\Users\User\Desktop\SITO\Sito indipendente          │
│     git add . && git commit -m "msg" && git push            │
│     (Render fa deploy automatico da main)                   │
└─────────────────────────────────────────────────────────────┘

┌─ LOGIN ─────────────────────────────────────────────────────┐
│  Utente: Ing   Password: Ing                                │
│  "Ricordami" salva in localStorage, auto-login al reload    │
└─────────────────────────────────────────────────────────────┘

┌─ AI (Chat Assistente) ──────────────────────────────────────┐
│  GitHub Pages:                                               │
│   1. text.pollinations.ai (gratis, no key)                   │
│   2. Fallback: gen.pollinations.ai + key + model:"openai"   │
│                                                             │
│  Render (server.js proxy): /api/chat → stessa logica        │
│  System prompt: risposte ITA, concise, [Fonte: TitoloFile]  │
│  Download: [DOWNLOAD:ID] nel testo della risposta           │
│  LaTeX: cleanLatex() converte tutto in Unicode pre-display  │
└─────────────────────────────────────────────────────────────┘

┌─ ARCHITETTURA (dopo v2.6.0) ────────────────────────────────┐
│  GITHUB PAGES (root):                                       │
│    index.html          → solo HTML + CDN + <script module>  │
│    style.css           → tutti gli stili custom             │
│    js/config.js        → Firebase, AI key, Telegram, i18n   │
│    js/app.js           → ES module: tutta la logica JS     │
│                                                             │
│  RENDER (Sito indipendente/client):                         │
│    index.html          → solo HTML + CDN + <script module>  │
│    style.css           → copia identica                     │
│    firebase-shim.js    → shim ES module per nedb API        │
│    js/config.js        → solo i18n                          │
│    js/app.js           → ES module: importa firebase-shim   │
│                                                             │
│  VERSIONI: v2.5.1 (backup pre-modernizzazione)              │
│            v2.6.0 (split in moduli)                         │
└─────────────────────────────────────────────────────────────┘

┌─ TELEGRAM ──────────────────────────────────────────────────┐
│  Bot token per notifiche broadcast:                          │
│  GitHub Pages: hardcoded in js/config.js                     │
│  Render: via env TELEGRAM_BOT_TOKEN → localStorage           │
│  Bot: @HubEngineerBot                                       │
│  Broadcast: a tutti i subscribers con chatId salvato         │
└─────────────────────────────────────────────────────────────┘

┌─ FIRESTORE (solo GitHub Pages) ─────────────────────────────┐
│  API Key: AIzaSyA5nEN5QiDwSxIShJdwmpbEoHKkEkNLIxM          │
│  Project: hub-ingegneria                                    │
│  Collezioni: excelHub, textHub, notesHub, newsHub,          │
│              teamHub, subscribers, historyHub, archiveFolders│
│  NOTA: Render usa nedb (locale) con /api/*                  │
│  Migrazione one-shot: pulsante "Importa da Firebase"        │
└─────────────────────────────────────────────────────────────┘

┌─ COMANDI GIT UTILI ─────────────────────────────────────────┐
│  git status                                                 │
│  git add -A                                                 │
│  git commit -m "v2.6.0: descrizione"                        │
│  git push                                                   │
│  git tag v2.6.0 && git push origin v2.6.0                   │
│  git log --oneline -5                                       │
└─────────────────────────────────────────────────────────────┘
